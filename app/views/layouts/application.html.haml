!!! 5

= raw '<!--[if lt IE 7 ]> <html class="ie6" lang="en" ng-app> <![endif]-->'
= raw '<!--[if IE 7 ]>    <html class="ie7" lang="en" ng-app> <![endif]-->'
= raw '<!--[if IE 8 ]>    <html class="ie8" lang="en" ng-app> <![endif]-->'
= raw '<!--[if (gte IE 9)|!(IE)]><!--> <html lang="en" ng-app> <!--<![endif]-->'

%head
  %meta( charset="utf-8" )
  %meta( name="viewport" content="width=device-width, initial-scale=1.0" )

  %title= "kelasiTline#{content_for?(:title) ? titleize(yield :title) : ''}"

  = stylesheet_link_tag "application", :media => "all"

  = javascript_include_tag "vendor/custom.modernizr"
  = csrf_meta_tags

  - if content_for? :head
    = yield :head

%body
  - if Rails.env.development?
    %pre( style="position:fixed;bottom:0" )= session.to_yaml

  .row
    .large-12.large-centered.columns
      %header
        .panel
          %h1 Timeline of Kelasi project
      %section#body= yield

  = javascript_include_tag "application"
  - if content_for? :script
    = yield :script

= raw "</html>"
